## What changes

<!-- One paragraph: what is different once this lands, and why. -->

## Gopnik verdict

<!--
From the gate run against this change. A pull request without a verdict is a
change nobody checked - write that here rather than leaving the section empty.
Stage 2 runs against the deployed instance, so it cannot run in CI; if it was
not run, say which surfaces are unproven.
-->

- **Verdict:** READY / NOT READY
- **Stage 1:**
- **Stage 2:**

## Claims

<!--
Anything this change asserts rather than demonstrates: a diagnosis, a closed
class of bug, a "this cannot happen again". The gate proves the change works
and will not touch these - gopnik-critic is what attacks them. Leave empty if
the change asserts nothing.
-->
